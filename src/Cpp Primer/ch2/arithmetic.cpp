#include <iostream>
#include <cstdio>

using namespace std;

/*

基本内置类型
1 算术类型
    算术类型分为整型和浮点型
1）整型 integral type
    bool
    char
    short
    int
    long
    long long

    不常见的还有：wchar_t, char16_t, char32_t
2）浮点型 
    float
    double
    long double

3）带符号类型和无符号类型
    除 bool 和扩展的字符型外，其余整型可以划分为带符号的（signed）
    和无符号（unsigned）两种

    类型 short，int，long，long long 默认都是带符号的

    与其他整型不同，字符型被分为：char, signed char, unsigned char
    类型 char 实际是带符号还是无符号由编译器决定


字面值常量
    整型和浮点型字面量

定义变量

变量作用域
    全局变量
    局部变量

常量
    const


*/
int main() {
    int j;

    int j = 2;

    return 0;
}